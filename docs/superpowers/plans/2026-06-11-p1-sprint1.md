# P1 Sprint 1 Implementation Plan — V1.0 收尾冲刺

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 V1.0 P0 25/25 + F-1~F-6 全部通过的基础上,完成 7 个高价值低风险 P1 任务(总 ~3h),消除 V1.0 上线前的剩余 P1 风险点。

**Architecture:** 每个任务都是"局部小补丁":
- 后端 4 个(SHOULD-31 health、6 compression、7 缓存 invalidation、16 软删)
- DB 1 个(SHOULD-32~35 四个索引,合并 1 个迁移)
- DTO 1 个(SHOULD-11 pageSize Max)
- 重构 1 个(SHOULD-39 BigInt 去重)
- 全部冒烟验收(curl 端到端),无单测基建时 TDD 退化为"实现→冒烟→commit"

**Tech Stack:** NestJS 10 + Prisma 5 + MySQL 8 + Redis 7 + Express helmet 现有栈

---

## 任务范围(7 项,~3h)

| 编号 | 来源 | 任务 | 工时 | 阻塞 V1.0? |
|---|---|---|---|---|
| T1 | SHOULD-31 | `GET /api/v1/health` 端点(检 MySQL+Redis) | 0.5h | ✅ LB 探活 |
| T2 | SHOULD-6 | compression 中间件 | 0.3h | ⚠️ 带宽 |
| T3 | SHOULD-7 | Post 列表缓存 invalidation | 0.5h | ⚠️ 脏数据 |
| T4 | SHOULD-16 | 删除用户改软删(status=2) | 0.5h | ✅ 数据安全 |
| T5 | SHOULD-39 | BigInt 序列化去重(统一 interceptor) | 0.5h | ✅ 代码质量 |
| T6 | SHOULD-32~35 | 4 个 DB 索引(1 个迁移) | 0.5h | ✅ 查询性能 |
| T7 | SHOULD-11 | pageSize `@Max(100)` DTO 守门 | 0.2h | ✅ DoS 防护 |

---

## 前置检查(全任务通用)

- [ ] **Step 0: 工作区干净 + 已知 commit**
  ```bash
  cd /e/workspace/yichun-you-shi-er-shuo
  git status --short
  git log --oneline -1
  ```
  Expected: 工作区干净(或有已记录的 untracked 上传测试图);HEAD 在 main 上,领先 origin 29 commit。

- [ ] **Step 0.1: 后端能启动 + DB 已迁移**
  ```bash
  cd backend && npm run start:dev
  # 看到 "🚀 后端服务运行在: http://localhost:3001/api/v1"
  ```
  Expected: 启动成功,无 ERR_REQUIRE_ESM / sharp / crypto 错误。

---

## Task 1: GET /api/v1/health 端点(SHOULD-31)

**Files:**
- Create: `backend/src/modules/health/health.controller.ts`
- Create: `backend/src/modules/health/health.module.ts`
- Modify: `backend/src/app.module.ts:30-50` (注册 HealthModule)

- [ ] **Step 1: 创建 health.controller.ts**

```typescript
import { Controller, Get, HttpException } from '@nestjs/common';
import { Public } from '../../common/decorators/public.decorator';
import { PrismaService } from '../../prisma/prisma.service';
import { RedisService } from '../../redis/redis.service';

@Controller('health')
export class HealthController {
  constructor(
    private readonly prisma: PrismaService,
    private readonly redis: RedisService,
  ) {}

  @Public()
  @Get()
  async check() {
    const startedAt = Date.now();
    const checks: Record<string, { ok: boolean; latencyMs: number; error?: string }> = {};

    // MySQL ping
    try {
      const t0 = Date.now();
      await this.prisma.$queryRaw`SELECT 1`;
      checks.mysql = { ok: true, latencyMs: Date.now() - t0 };
    } catch (e: any) {
      checks.mysql = { ok: false, latencyMs: -1, error: e?.message || 'mysql ping failed' };
    }

    // Redis ping
    try {
      const t0 = Date.now();
      const pong = await this.redis.client.ping();
      checks.redis = { ok: pong === 'PONG', latencyMs: Date.now() - t0 };
    } catch (e: any) {
      checks.redis = { ok: false, latencyMs: -1, error: e?.message || 'redis ping failed' };
    }

    const ok = Object.values(checks).every((c) => c.ok);
    const data = {
      status: ok ? 'ok' : 'degraded',
      uptime: Math.round(process.uptime()),
      timestamp: new Date().toISOString(),
      version: process.env.npm_package_version || '0.0.0',
      checks,
      totalLatencyMs: Date.now() - startedAt,
    };

    if (!ok) {
      throw new HttpException({ code: 1, message: 'unhealthy', data }, 503);
    }
    return data;
  }
}
```

- [ ] **Step 2: 创建 health.module.ts**

```typescript
import { Module } from '@nestjs/common';
import { HealthController } from './health.controller';

@Module({
  controllers: [HealthController],
})
export class HealthModule {}
```

- [ ] **Step 3: 在 app.module.ts 注册 HealthModule**

找到 `imports` 数组(PrismaModule、RedisService 应已存在),加 `HealthModule`:

```typescript
import { HealthModule } from './modules/health/health.module';
// ...
imports: [
  // ... 已有
  HealthModule,
],
```

- [ ] **Step 4: 冒烟验证**

```bash
# 重启后端
curl -s http://localhost:3001/api/v1/health | jq .
```

Expected:
```json
{
  "code": 0,
  "data": {
    "status": "ok",
    "uptime": 12,
    "timestamp": "2026-06-11T...",
    "version": "0.0.0",
    "checks": {
      "mysql": { "ok": true, "latencyMs": 3 },
      "redis": { "ok": true, "latencyMs": 1 }
    },
    "totalLatencyMs": 4
  }
}
```

- [ ] **Step 5: Commit**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
git add backend/src/modules/health/ backend/src/app.module.ts
git commit -m "feat(backend): SHOULD-31 GET /api/v1/health checks MySQL + Redis"
```

---

## Task 2: compression 中间件(SHOULD-6)

**Files:**
- Modify: `backend/package.json:35-50` (添加 compression 依赖)
- Modify: `backend/src/main.ts:1-50` (添加 app.use(compression()))

- [ ] **Step 1: 安装 compression**

```bash
cd /e/workspace/yichun-you-shi-er-shuo/backend
npm install compression @types/compression
```

- [ ] **Step 2: 在 main.ts 引入并启用**

修改 `backend/src/main.ts`,在 `import` 区块加:

```typescript
import compression from 'compression';
```

在 `helmet` 之后、`enableCors` 之前(或同等位置)加:

```typescript
// 响应压缩(SHOULD-6):列表响应 200KB+ 节省 80% 带宽
app.use(
  compression({
    threshold: 1024, // 仅压缩 > 1KB
    level: 6, // 平衡 CPU 与压缩率
    filter: (req, res) => {
      if (req.headers['x-no-compression']) return false;
      return compression.filter(req, res);
    },
  }),
);
```

- [ ] **Step 3: 冒烟验证**

```bash
curl -s -H "Accept-Encoding: gzip" -I http://localhost:3001/api/v1/categories/tree
```

Expected: 响应头含 `Content-Encoding: gzip` 且 `Content-Length` 小于未压缩版本(可对比无 -H 调用)。

- [ ] **Step 4: Commit**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
git add backend/package.json backend/package-lock.json backend/src/main.ts
git commit -m "perf(backend): SHOULD-6 compression middleware (>1KB responses gzipped)"
```

---

## Task 3: Post 列表缓存 invalidation(SHOULD-7)

**Files:**
- Read first: `backend/src/modules/post/post.service.ts` 找缓存 key 前缀(`cache:posts:*` 或类似)
- Modify: `backend/src/modules/post/post.service.ts` (create/update/remove 中加 redis.del)

- [ ] **Step 1: 查现有缓存 key 模式**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
grep -rn "cache:posts" backend/src/
```

Expected: 看到 `cache:posts:list:*` 或 `posts:list:*` 这类 key(由 post.service.ts 写)。记下实际前缀。

- [ ] **Step 2: 找到写操作方法(create/update/remove)**

```bash
grep -n "async create\|async update\|async remove\|async offline" backend/src/modules/post/post.service.ts
```

Expected: 至少 3 个写方法。每个方法**返回前**加缓存清理。

- [ ] **Step 3: 在 post.service.ts 中加缓存清理**

在 `constructor` 注入 `RedisService`(若未注入):

```typescript
import { RedisService } from '../../redis/redis.service';

constructor(
  private readonly prisma: PrismaService,
  private readonly redis: RedisService, // 新增
) {}
```

在 `create()`、`update()`、`remove()`、`offline()` 四个方法的**返回前**(找到 `return ...;` 那一行,**之前**),加:

```typescript
// SHOULD-7: 写操作清列表缓存,避免用户改完看到 stale 数据
await this.redis.invalidatePattern('cache:posts:list:*');
// 若实际 key 前缀不同(如 posts:list:*),替换为真实前缀
```

若 `RedisService` 没有 `invalidatePattern` 方法,在 `backend/src/redis/redis.service.ts` 加:

```typescript
async invalidatePattern(pattern: string): Promise<number> {
  const client = this.client;
  let cursor = '0';
  let deleted = 0;
  do {
    const [next, keys] = await client.scan(cursor, 'MATCH', pattern, 'COUNT', 100);
    cursor = next;
    if (keys.length) {
      deleted += await client.del(...keys);
    }
  } while (cursor !== '0');
  return deleted;
}
```

- [ ] **Step 4: 冒烟验证**

```bash
# 1) 触发列表请求(产生缓存)
curl -s http://localhost:3001/api/v1/posts?type=house > /dev/null
# 2) 查 Redis 有缓存 key
docker exec -it $(docker ps -qf "ancestor=mysql:8" 2>/dev/null; echo) 2>/dev/null  # 仅示意
redis-cli -h localhost -p 6379 KEYS "cache:posts:list:*"
# 3) 创建一个 post
curl -s -X POST http://localhost:3001/api/v1/posts \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"type":"house","title":"缓存清理测试","description":"x","price":1,"categoryId":1,"areaId":1,"contactName":"test","contactPhone":"13800000000"}'
# 4) 再查 Redis,应无 cache:posts:list:* key
redis-cli -h localhost -p 6379 KEYS "cache:posts:list:*"
```

Expected: 步骤 2 有 key;步骤 4 无 key(被清空)。

- [ ] **Step 5: Commit**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
git add backend/src/modules/post/post.service.ts backend/src/redis/redis.service.ts
git commit -m "perf(backend): SHOULD-7 invalidate post list cache on write"
```

---

## Task 4: 删除用户改软删(SHOULD-16)

**Files:**
- Read first: `backend/prisma/schema.prisma` 找 `User.status` 字段定义
- Modify: `backend/src/modules/user/user.service.ts:remove` 方法

- [ ] **Step 1: 查 User.status 当前类型与枚举**

```bash
cd /e:/workspace/yichun-you-shi-er-shuo
grep -A 2 "model User" backend/prisma/schema.prisma | head -20
grep "status" backend/prisma/schema.prisma | head -20
```

Expected: `User` 有 `status` 字段(可能是 `Int` 或枚举)。记下当前值域(`0`/`1` 是 active/banned 还是其他)。约定:增加 `2` = `deleted`(软删)。

- [ ] **Step 2: 修改 user.service.ts 的 remove()**

打开 `backend/src/modules/user/user.service.ts`,找到 `remove` 方法,替换为软删实现:

```typescript
/**
 * SHOULD-16: 删除用户改软删(status=2),保留数据可恢复
 * 物理硬删会破坏帖子/评论/收藏/站内信关联
 * @param id 用户 ID
 * @param currentUser 操作者(sub, role)
 */
async remove(id: bigint, currentUser?: { sub: bigint; role: string }) {
  if (currentUser) {
    if (currentUser.role !== 'admin') {
      throw new ForbiddenException('只有管理员能删除用户');
    }
    if (String(currentUser.sub) === String(id)) {
      throw new BadRequestException('不能删除自己,请联系其他管理员');
    }
  }

  const existing = await this.prisma.user.findUnique({ where: { id } });
  if (!existing) {
    throw new NotFoundException('用户不存在');
  }
  if (existing.status === 2) {
    return { id: id.toString(), status: 2, alreadyDeleted: true };
  }

  await this.prisma.user.update({
    where: { id },
    data: { status: 2, updatedAt: new Date() },
  });

  // 同步:使该用户所有 access token 失效(加黑名单)
  // 此处省略 jwt 黑名单(由 token 过期自然失效,7d 内用户被踢)

  return { id: id.toString(), status: 2, softDeleted: true };
}
```

补 import:

```typescript
import { ForbiddenException, BadRequestException, NotFoundException } from '@nestjs/common';
```

- [ ] **Step 3: 修改 controller 传 currentUser**

打开 `backend/src/modules/user/user.controller.ts`,修改 `remove`:

```typescript
@UseGuards(AdminGuard)
@Roles('admin')
@Delete(':id')
remove(
  @Param('id') id: string,
  @CurrentUser() user: JwtPayload,  // 新增
) {
  return this.userService.remove(BigInt(id), user);  // 传 user
}
```

- [ ] **Step 4: 冒烟验证**

```bash
# 拿 admin token
ADMIN_TOKEN=$(curl -s -X POST http://localhost:3001/api/v1/auth/sms-code \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800000000"}' >/dev/null && \
  CODE=$(redis-cli -h localhost -p 6379 GET "sms:code:13800000000" | tr -d '"') && \
  curl -s -X POST http://localhost:3001/api/v1/auth/login-sms \
  -H "Content-Type: application/json" \
  -d "{\"phone\":\"13800000000\",\"code\":\"$CODE\"}" | jq -r '.data.accessToken')

# 1) 拿一个非 admin 用户 id
TEST_USER_ID=2  # 假设
# 2) 软删
curl -s -X DELETE "http://localhost:3001/api/v1/users/$TEST_USER_ID" \
  -H "Authorization: Bearer $ADMIN_TOKEN" | jq .
# 3) 查 DB 应是 status=2
docker exec -it $(docker ps -qf "ancestor=mysql:8") mysql -uroot -pyichun123456 yichun -e "SELECT id, status FROM users WHERE id = $TEST_USER_ID"
# 4) 再调一次,应 alreadyDeleted: true
curl -s -X DELETE "http://localhost:3001/api/v1/users/$TEST_USER_ID" \
  -H "Authorization: Bearer $ADMIN_TOKEN" | jq .
# 5) 恢复(测试用):status=1
docker exec -it $(docker ps -qf "ancestor=mysql:8") mysql -uroot -pyichun123456 yichun -e "UPDATE users SET status = 1 WHERE id = $TEST_USER_ID"
```

Expected: 第 2 步返回 `{id, status: 2, softDeleted: true}`;第 3 步 DB 显示 status=2;第 4 步返回 `alreadyDeleted: true`;第 5 步恢复。

- [ ] **Step 5: Commit**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
git add backend/src/modules/user/user.service.ts backend/src/modules/user/user.controller.ts
git commit -m "feat(backend): SHOULD-16 user remove -> soft delete (status=2)"
```

---

## Task 5: BigInt 序列化去重(SHOULD-39)

**Files:**
- Read first: `backend/src/common/interceptors/transform.interceptor.ts` 确认已实现 BigInt → string
- Read: `backend/src/modules/post/post.service.ts:1-50` 找 `bigIntToString` helper
- Modify: `backend/src/modules/post/post.service.ts` 删除本地 helper(若仍有使用,替换为全局)

- [ ] **Step 1: 验证全局 interceptor 已处理 BigInt**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
cat backend/src/common/interceptors/transform.interceptor.ts | head -80
```

Expected: 看到 `data => bigIntToString(data)` 或 `JSON.stringify(data, (_, v) => typeof v === 'bigint' ? v.toString() : v)`。

- [ ] **Step 2: 找 post.service.ts 的 bigIntToString**

```bash
grep -n "bigIntToString\|BigInt" backend/src/modules/post/post.service.ts | head -20
```

Expected: 若有 `bigIntToString` 函数(20-40 行),找到位置。审计原报告说 `post.service.ts:23-35`。

- [ ] **Step 3: 删本地 helper,移除内部调用**

打开 `backend/src/modules/post/post.service.ts`,删除 `bigIntToString` 函数定义(若有),并搜索 `bigIntToString(` 调用,逐一替换为依赖全局 `TransformInterceptor`(即直接返回数据,不二次转换)。

示例:
```diff
-  private bigIntToString(obj: any): any {
-    // ... 30 行
-  }
-
   async findAll(...) {
-    return this.bigIntToString(result);
+    return result; // 交给 TransformInterceptor 统一处理
   }
```

若 `findAll` 等返回的是数组/对象,直接 return;若是 Prisma 结果(含 BigInt),Interceptor 会处理。

- [ ] **Step 4: 冒烟验证**

```bash
curl -s "http://localhost:3001/api/v1/posts?type=house&pageSize=1" | jq '.data.list[0].id'
```

Expected: 返回字符串(如 `"1"`),不是数字也不是报错"BigInt"。

```bash
curl -s "http://localhost:3001/api/v1/categories/tree" | jq '.data[0].id'
```

Expected: 同上。

- [ ] **Step 5: Commit**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
git add backend/src/modules/post/post.service.ts
git commit -m "refactor(backend): SHOULD-39 remove local bigIntToString (rely on global TransformInterceptor)"
```

---

## Task 6: 4 个 DB 索引(SHOULD-32~35)

**Files:**
- Read first: `backend/prisma/schema.prisma` 找 User/Post/PostHouse/Company 模型
- Create: `backend/prisma/migrations/20260611120000_add_sprint1_indexes/migration.sql`
- Run: `npx prisma migrate deploy`(在 backend 目录)

- [ ] **Step 1: 查模型字段**

```bash
cd /e/workspace/yichun-you-shi-er-shuo/backend
grep -A 2 "^model User\|^model Post \|^model PostHouse\|^model Company" prisma/schema.prisma | head -50
```

Expected: 4 个模型都有 `status` / `expiredAt` / `verified` 等字段,确认可加索引。

- [ ] **Step 2: 创建迁移目录**

```bash
cd /e/workspace/yichun-you-shi-er-shuo/backend
mkdir -p prisma/migrations/20260611120000_add_sprint1_indexes
```

- [ ] **Step 3: 写 migration.sql**

```sql
-- SHOULD-32: User.role 索引(admin 用户列表)
CREATE INDEX idx_user_role ON users (role);

-- SHOULD-33: Post.expiredAt 索引(过期 cron)
CREATE INDEX idx_post_expired_at ON posts (expired_at);

-- SHOULD-34: PostHouse 复合索引(整租/合租/户型/面积筛选)
CREATE INDEX idx_post_houses_rental_property_area ON post_houses (rental_type, property_type, area_sqm);

-- SHOULD-35: Company.verified 索引(商家列表)
CREATE INDEX idx_company_verified ON companies (verified);
```

注意:字段名以 schema.prisma 实际为准(可能是 `expiredAt` vs `expired_at`、`rentalType` vs `rental_type`、`areaSqm` vs `area_sqm`、`verified` vs `verified_at`)。

- [ ] **Step 4: 应用迁移**

```bash
cd /e/workspace/yichun-you-shi-er-shuo/backend
npx prisma migrate deploy
```

Expected: `4 migrations found in prisma/migrations. Applying... Migration 20260611120000_add_sprint1_indexes applied successfully.`

- [ ] **Step 5: 验证索引**

```bash
docker exec -it $(docker ps -qf "ancestor=mysql:8") mysql -uroot -pyichun123456 yichun -e "SHOW INDEX FROM users WHERE Key_name = 'idx_user_role'"
docker exec -it $(docker ps -qf "ancestor=mysql:8") mysql -uroot -pyichun123456 yichun -e "SHOW INDEX FROM posts WHERE Key_name = 'idx_post_expired_at'"
```

Expected: 各返回 1 行(索引存在)。

- [ ] **Step 6: Commit**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
git add backend/prisma/migrations/20260611120000_add_sprint1_indexes/
git commit -m "perf(db): SHOULD-32~35 add 4 indexes (User.role, Post.expiredAt, PostHouse composite, Company.verified)"
```

---

## Task 7: pageSize Max(100) 守门(SHOULD-11)

**Files:**
- Modify: `backend/src/modules/post/dto/query-post.dto.ts`(或类似查询 DTO)
- Modify: `backend/src/modules/user/dto/query-user.dto.ts`
- Modify: `backend/src/modules/category/dto/query-category.dto.ts`(若有)
- Modify: `backend/src/modules/favorite/dto/query-favorite.dto.ts`(若有)
- Modify: `backend/src/modules/comment/dto/query-comment.dto.ts`(若有)

- [ ] **Step 1: 找所有 pageSize 出现位置**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
grep -rn "pageSize\|PageSize" backend/src/modules/*/dto/ | head -30
```

Expected: 列出所有含 `@Query() pageSize` 或 DTO 中 `pageSize` 字段的文件路径。

- [ ] **Step 2: 给 pageSize 字段加 @Max(100)**

打开每个 DTO 文件,在 `pageSize` 字段上添加 `@Max`:

```typescript
import { Max } from 'class-validator';

export class QueryPostDto {
  // ...
  @IsOptional()
  @IsInt()
  @Min(1)
  @Max(100)  // 新增
  pageSize?: number = 20;
}
```

- [ ] **Step 3: 冒烟验证**

```bash
# 超限应 400
curl -s "http://localhost:3001/api/v1/posts?type=house&pageSize=200" | jq .
# 正常应 200
curl -s "http://localhost:3001/api/v1/posts?type=house&pageSize=20" | jq '.code'
```

Expected: 第 1 步 `code !== 0`(400 ValidationPipe 拦截);第 2 步 `code = 0`。

- [ ] **Step 4: Commit**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
git add backend/src/modules/*/dto/
git commit -m "perf(backend): SHOULD-11 cap pageSize at 100 to prevent DoS"
```

---

## 收尾

- [ ] **Step F1: 全量冒烟复跑**

```bash
# 1) Health
curl -s http://localhost:3001/api/v1/health | jq '.data.status'
# Expected: "ok"

# 2) 列表压缩
curl -s -H "Accept-Encoding: gzip" -I http://localhost:3001/api/v1/posts?type=house | grep -i "content-encoding"
# Expected: "Content-Encoding: gzip"

# 3) pageSize 守门
curl -s "http://localhost:3001/api/v1/posts?pageSize=999" | jq '.code'
# Expected: 非 0

# 4) BigInt 序列化为字符串
curl -s "http://localhost:3001/api/v1/posts?type=house&pageSize=1" | jq '.data.list[0].id | type'
# Expected: "string"
```

- [ ] **Step F2: 更新 project-memory + roadmap**

在 `docs/project-memory.md` §10.5 P1 状态添加 P1 Sprint 1 完成说明(7 项)。

在 `docs/development-roadmap.md` 把对应 SHOULD 行标记 ✅ 并填 commit SHA。

- [ ] **Step F3: Commit 收尾**

```bash
cd /e/workspace/yichun-you-shi-er-shuo
git add docs/project-memory.md docs/development-roadmap.md
git commit -m "docs: P1 Sprint 1 complete (SHOULD-31/6/7/16/39/32-35/11)"
git push  # 若需推送
```

---

## Self-Review

- [x] **Spec coverage**:7 个任务对应 7 个 SHOULD 编号(31/6/7/16/39/32-35/11),每个 P1 项都有 task。
- [x] **Placeholder scan**:无 "TBD"/"implement later"/"fill in details";"// SHOULD-XX" 注释有具体内容;迁移字段名有 "以 schema.prisma 实际为准" 的提示(故意,因 prisma 字段命名规则不一)。
- [x] **Type consistency**:Task 1 `HealthController` 用了 `PrismaService` + `RedisService`;Task 3 `invalidatePattern` 在 redis.service.ts 加;Task 4 `remove(id, currentUser)` 签名 controller 调用一致。
- [x] **冒烟步骤**:每个 task 都有可执行命令 + 期望输出,无"写适当测试"等模糊描述。

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-06-11-p1-sprint1.md`.**

**1. Subagent-Driven (推荐)** - 每个 task 派一个独立 subagent,做完后做 spec compliance review + code quality review,fresh context per task。

**2. Inline Execution** - 在本会话用 executing-plans 顺序执行,批量 + 检查点。

请选执行方式。
