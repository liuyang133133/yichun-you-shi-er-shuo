# P1 Sprint 4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close 3 P1 items to finish V1.0 production readiness: quality gate (CI), operations efficiency (batch audit), and timezone consistency (Asia/Shanghai end-to-end).

**Architecture:**
- **T1 (SHOULD-42)** adds `.github/workflows/ci.yml` with 3 parallel jobs (backend build / frontend lint+build / admin lint+build). No service containers, no tests. Backend lint commented out (no `.eslintrc` exists).
- **T2 (SHOULD-27)** adds 2 batch endpoints `POST /admin/posts/audit-batch` and `POST /admin/posts/offline-batch`. Each runs `prisma.post.updateMany` (single SQL) + one summary `AuditLog` row in a single `$transaction` (mirroring `purgeOldDeleted` pattern). Frontend admin gets checkbox column + batch action bar.
- **T3 (SHOULD-36)** is **frontend-only**. Backend `transform.interceptor.ts:43` already returns ISO 8601 Z. Add `frontend/src/lib/date.ts` + `admin/src/lib/date.ts` with `formatDateTime` / `formatDate` / `formatRelative` using native `Intl` API (no `dayjs` dep). Hardcode `Asia/Shanghai` (伊春 黑龙江 +8). Replace 9 call sites of `toLocaleString('zh-CN')` without `timeZone` option.

**Tech Stack:** GitHub Actions v4 + NestJS 10 + Prisma 5 + Next.js 15 + native `Intl` API

**Total commits:** ~3-6 (each task = 1-2 commits + reviews), ~5h wall-clock

**Order rationale:**
- T1 first (1h, lowest risk, no application code touched)
- T2 second (1.5h, narrow blast radius, mirrors existing `purgeOldDeleted` pattern)
- T3 last (2h, most files touched but purely frontend display — 9 files + 2 new helpers)

---

## Task 1: SHOULD-42 GitHub Actions CI

**Files:**
- Create: `.github/workflows/ci.yml`
- (no application code changes)

- [ ] **Step 1: Create workflow file**

Create `e:\workspace\yichun-you-shi-er-shuo\.github\workflows\ci.yml`:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Generate Prisma client
        run: npx prisma generate
      - name: Build
        run: npm run build
      # 后端 lint script 引用了不存在的 eslint 配置,暂时不跑
      # - name: Lint
      #   run: npm run lint

  frontend:
    name: Frontend lint + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Type check
        run: npm run type-check
      - name: Build
        run: npm run build

  admin:
    name: Admin lint + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: admin
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: admin/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Type check
        run: npm run type-check
      - name: Build
        run: npm run build
```

- [ ] **Step 2: Validate YAML syntax locally**

```bash
# 用 node 简单校验 YAML 解析正确
node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); console.log('lines:', y.split('\n').length); console.log('jobs:', (y.match(/^  \w+:$/gm) || []).join(','));"
```
Expected: 输出 jobs 列表含 `backend`, `frontend`, `admin`。

- [ ] **Step 3: Verify frontend type-check / build works locally**

```bash
cd frontend
npm run lint && npm run type-check && npm run build
cd ../admin
npm run lint && npm run type-check && npm run build
```
Expected: 全部 PASS(若发现依赖缺失,先 `npm install`)。如果原本就有 build 错误,需先修才能让 CI 绿。

- [ ] **Step 4: Verify backend build works locally**

```bash
cd backend
npm install
npx prisma generate
npm run build
```
Expected: 编译成功,`backend/dist/` 有 `main.js`。

- [ ] **Step 5: Smoke — git status + final review**

```bash
cd ..
git status
# 应该只看到 .github/workflows/ci.yml 一个新文件
```

- [ ] **Step 6: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: SHOULD-42 GitHub Actions CI (backend build + frontend/admin lint+build, 3 parallel jobs)"
```

---

## Task 2: SHOULD-27 Batch Audit / Batch Offline

**Files:**
- Modify: `backend/src/modules/admin/post/admin-post.service.ts:46-141` (add `auditBatch` + `offlineBatch` after `offline()`)
- Modify: `backend/src/modules/admin/post/admin-post.controller.ts:44-72` (add 2 new routes)
- Modify: `admin/src/app/posts/page.tsx` (add checkbox column + batch action bar)

- [ ] **Step 1: Read existing service & controller**

Read `backend/src/modules/admin/post/admin-post.service.ts` and `admin-post.controller.ts`. Note exact line numbers and how `pass` / `reject` / `offline` use `$transaction` + AuditLog.

- [ ] **Step 2: Add `auditBatch` method to service**

After the existing `offline()` method (around line 141), add:

```ts
async auditBatch(
  adminId: bigint,
  ids: bigint[],
  action: 'pass' | 'reject',
  reason?: string,
) {
  if (ids.length === 0) {
    throw new BadRequestException('ids 不能为空');
  }
  if (ids.length > 200) {
    throw new BadRequestException('单次最多 200 条');
  }
  if (action === 'reject' && (!reason || !reason.trim())) {
    throw new BadRequestException('批量拒绝时必须填写理由');
  }

  // 预查:只对 auditStatus=pending 的生效
  const targets = await this.prisma.post.findMany({
    where: { id: { in: ids }, auditStatus: 'pending' },
    select: { id: true },
  });
  if (targets.length === 0) {
    throw new BadRequestException('没有可审核的帖子(可能已审核或不存在)');
  }
  const targetIds = targets.map((t) => t.id);

  const op = action === 'pass'
    ? { auditStatus: 'passed', status: 'active', auditReason: reason ? `[batch pass] ${reason}` : null }
    : { auditStatus: 'rejected', status: 'rejected', auditReason: reason! };

  const updateOp = this.prisma.post.updateMany({
    where: { id: { in: targetIds } },
    data: op,
  });

  const logOp = this.prisma.auditLog.create({
    data: {
      adminUserId: adminId,
      module: 'post',
      action: action === 'pass' ? 'audit_pass_batch' : 'audit_reject_batch',
      targetType: 'post',
      targetId: null,
      reason: reason || null,
      metadata: { ids: targetIds.map(String), count: targetIds.length },
    },
  });

  await this.prisma.$transaction([updateOp, logOp]);
  return { success: targetIds.length };
}
```

- [ ] **Step 3: Add `offlineBatch` method to service**

Add after `auditBatch`:

```ts
async offlineBatch(adminId: bigint, ids: bigint[], reason: string) {
  if (ids.length === 0) {
    throw new BadRequestException('ids 不能为空');
  }
  if (ids.length > 200) {
    throw new BadRequestException('单次最多 200 条');
  }
  if (!reason || !reason.trim()) {
    throw new BadRequestException('必须填写下架理由');
  }

  // 强制下架:不限制状态
  const targets = await this.prisma.post.findMany({
    where: { id: { in: ids } },
    select: { id: true },
  });
  if (targets.length === 0) {
    throw new BadRequestException('没有可下架的帖子');
  }
  const targetIds = targets.map((t) => t.id);

  const updateOp = this.prisma.post.updateMany({
    where: { id: { in: targetIds } },
    data: {
      status: 'rejected',
      auditStatus: 'rejected',
      auditReason: `[强制下架] ${reason}`,
    },
  });

  const logOp = this.prisma.auditLog.create({
    data: {
      adminUserId: adminId,
      module: 'post',
      action: 'offline_batch',
      targetType: 'post',
      targetId: null,
      reason,
      metadata: { ids: targetIds.map(String), count: targetIds.length },
    },
  });

  await this.prisma.$transaction([updateOp, logOp]);
  return { success: targetIds.length };
}
```

Add `BadRequestException` import if not already.

- [ ] **Step 4: Add 2 new routes to controller**

After the existing `offline()` route (around line 72), add:

```ts
@Post('audit-batch')
@Roles('admin')
auditBatch(
  @CurrentUser('id') adminId: string,
  @Body() dto: { ids: string[]; action: 'pass' | 'reject'; reason?: string },
) {
  const ids = dto.ids.map((s) => BigInt(s));
  return this.adminPostService.auditBatch(
    BigInt(adminId),
    ids,
    dto.action,
    dto.reason,
  );
}

@Post('offline-batch')
@Roles('admin')
offlineBatch(
  @CurrentUser('id') adminId: string,
  @Body() dto: { ids: string[]; reason: string },
) {
  const ids = dto.ids.map((s) => BigInt(s));
  return this.adminPostService.offlineBatch(BigInt(adminId), ids, dto.reason);
}
```

Look at existing routes to mimic `@CurrentUser` decorator + `@Roles('admin')` + response shape.

- [ ] **Step 5: Restart backend + Smoke 1 — audit batch success**

```bash
cd backend && npm run start:dev > /tmp/sprint4-t2-boot.log 2>&1 &
sleep 10

# 拿 admin token
ADMIN_TOKEN=$(curl -s -X POST http://localhost:3001/api/v1/auth/login-password \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800000000","password":"123456"}' | jq -r '.data.accessToken // empty')

# 找 2 个 pending post
PENDING_IDS=$(curl -s "http://localhost:3001/api/v1/admin/posts?auditStatus=pending&pageSize=5" \
  -H "Authorization: Bearer $ADMIN_TOKEN" | jq -r '.data.items[].id' | head -2 | tr '\n' ',' | sed 's/,$//')
echo "Pending: $PENDING_IDS"

# 批量 pass
curl -s -X POST http://localhost:3001/api/v1/admin/posts/audit-batch \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"ids\":[$PENDING_IDS],\"action\":\"pass\"}" -w "\nHTTP:%{http_code}\n"
```

Expected: HTTP:200 + `{"success": N}`。

验证 audit log:
```bash
docker exec -i $(docker ps | grep mysql | awk '{print $1}' | head -1) \
  mysql -u yichun -p$(grep -oP 'mysql://\K[^:]+' backend/.env) yichun \
  -e "SELECT action, target_id, JSON_EXTRACT(metadata, '$.count') as cnt FROM audit_logs WHERE action='audit_pass_batch' ORDER BY created_at DESC LIMIT 1" 2>&1 | grep -v Warning
```
Expected: 1 row, `action=audit_pass_batch`, `cnt=N`。

- [ ] **Step 6: Smoke 2 — reject batch with reason**

```bash
# 找另外的 pending
PENDING_IDS=$(curl -s "http://localhost:3001/api/v1/admin/posts?auditStatus=pending&pageSize=5" \
  -H "Authorization: Bearer $ADMIN_TOKEN" | jq -r '.data.items[].id' | head -2 | tr '\n' ',' | sed 's/,$//')

# 批量 reject(必须带 reason)
curl -s -X POST http://localhost:3001/api/v1/admin/posts/audit-batch \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"ids\":[$PENDING_IDS],\"action\":\"reject\",\"reason\":\"批量测试拒绝\"}" -w "\nHTTP:%{http_code}\n"
```

Expected: HTTP:200。

- [ ] **Step 7: Smoke 3 — reject batch without reason → 400**

```bash
curl -s -X POST http://localhost:3001/api/v1/admin/posts/audit-batch \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"ids\":[1,2],\"action\":\"reject\"}" -w "\nHTTP:%{http_code}\n"
```

Expected: HTTP:400("批量拒绝时必须填写理由")。

- [ ] **Step 8: Smoke 4 — offline batch**

```bash
ACTIVE_IDS=$(curl -s "http://localhost:3001/api/v1/admin/posts?auditStatus=passed&pageSize=3" \
  -H "Authorization: Bearer $ADMIN_TOKEN" | jq -r '.data.items[].id' | head -2 | tr '\n' ',' | sed 's/,$//')

curl -s -X POST http://localhost:3001/api/v1/admin/posts/offline-batch \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"ids\":[$ACTIVE_IDS],\"reason\":\"批量测试下架\"}" -w "\nHTTP:%{http_code}\n"
```

Expected: HTTP:200。

- [ ] **Step 9: Smoke 5 — empty ids → 400**

```bash
curl -s -X POST http://localhost:3001/api/v1/admin/posts/audit-batch \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"ids":[],"action":"pass"}' -w "\nHTTP:%{http_code}\n"
```

Expected: HTTP:400("ids 不能为空")。

- [ ] **Step 10: Smoke 6 — 现有单条端点未破坏**

```bash
# 单条 audit 仍能用
SINGLE_ID=$(curl -s "http://localhost:3001/api/v1/admin/posts?auditStatus=pending&pageSize=1" \
  -H "Authorization: Bearer $ADMIN_TOKEN" | jq -r '.data.items[0].id')
curl -s -X POST http://localhost:3001/api/v1/admin/posts/$SINGLE_ID/audit \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action":"pass"}' -w "\nHTTP:%{http_code}\n"
```

Expected: HTTP:200。

- [ ] **Step 11: Commit backend changes**

```bash
git add backend/src/modules/admin/post/admin-post.service.ts \
        backend/src/modules/admin/post/admin-post.controller.ts
git commit -m "feat(backend): SHOULD-27 batch audit + batch offline (updateMany + summary audit log)"
```

**Note:** 前端 admin UI 改造可选,本任务**核心**是后端 API(commit 在这里)。前端 UI 可单独 sprint 优化。

---

## Task 3: SHOULD-36 Timezone Consistency

**Files:**
- Create: `frontend/src/lib/date.ts`
- Create: `admin/src/lib/date.ts`
- Modify: 9 frontend/admin files (replace `toLocaleString('zh-CN')` with new helpers)

- [ ] **Step 1: Create `frontend/src/lib/date.ts`**

```ts
// frontend/src/lib/date.ts
// 业务时区统一为 Asia/Shanghai(伊春 黑龙江 +8,无 DST)
// 零依赖,用原生 Intl API;不引入 dayjs/date-fns/luxon

const TZ = 'Asia/Shanghai';

export function formatDateTime(iso: string | Date): string {
  const d = typeof iso === 'string' ? new Date(iso) : iso;
  return d.toLocaleString('zh-CN', {
    timeZone: TZ,
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit',
  });
}

export function formatDate(iso: string | Date): string {
  const d = typeof iso === 'string' ? new Date(iso) : iso;
  return d.toLocaleDateString('zh-CN', { timeZone: TZ });
}

export function formatRelative(iso: string | Date): string {
  const d = typeof iso === 'string' ? new Date(iso) : iso;
  const diffSec = Math.floor((Date.now() - d.getTime()) / 1000);
  if (diffSec < 60) return '刚刚';
  if (diffSec < 3600) return `${Math.floor(diffSec / 60)} 分钟前`;
  if (diffSec < 86400) return `${Math.floor(diffSec / 3600)} 小时前`;
  if (diffSec < 604800) return `${Math.floor(diffSec / 86400)} 天前`;
  return formatDate(d);
}
```

- [ ] **Step 2: Create `admin/src/lib/date.ts`**

Copy-paste the same file to `admin/src/lib/date.ts`.

- [ ] **Step 3: Read & update `frontend/src/lib/utils.ts`**

Read `frontend/src/lib/utils.ts`. If it has existing `formatDate` / `timeAgo` (调研说 line 14-38),refactor:
- Remove the old `formatDate` and `timeAgo` from this file
- Update all call sites to import from `@/lib/date` instead

If existing functions are still used elsewhere, leave them but mark deprecated, or just refactor cleanly (3 callers: post-card, etc.).

- [ ] **Step 4: Update `frontend/src/components/post/post-card.tsx`**

Replace any `timeAgo(iso)` / `toLocaleDateString('zh-CN')` (line 29-37) with:
```ts
import { formatRelative } from '@/lib/date';
// ...
<span>{formatRelative(post.createdAt)}</span>
```

- [ ] **Step 5: Update `frontend/src/app/me/messages/page.tsx`**

Line 239: `toLocaleString('zh-CN', { month, day, hour, minute })` → `formatDateTime(createdAt)`

- [ ] **Step 6: Update `frontend/src/app/posts/[id]/post-detail-content.tsx`**

Three call sites (lines 242, 388, 517):
- 242: `toLocaleString('zh-CN')` → `formatDateTime`
- 388: `toLocaleDateString('zh-CN')` → `formatDate`
- 517: 留言时间 → `formatDateTime`

- [ ] **Step 7: Update `admin/src/app/companies/page.tsx` (line 48)**

`toLocaleDateString('zh-CN')` → `formatDate`

- [ ] **Step 8: Update `admin/src/app/users/page.tsx` (line 116)**

`toLocaleString('zh-CN', { month, day, hour, minute })` → `formatDateTime`

- [ ] **Step 9: Update `admin/src/app/posts/page.tsx` (line 139)**

`toLocaleString('zh-CN')` → `formatDateTime`

- [ ] **Step 10: Update `admin/src/app/reports/page.tsx` (line 108)**

`toLocaleString('zh-CN')` → `formatDateTime`

- [ ] **Step 11: NOT touch these (SEO/JSON-LD/sitemap)**

- `frontend/src/app/posts/[id]/page.tsx:110-137` (JSON-LD) — ISO Z 字符串
- `frontend/src/app/sitemap.ts:26` (Next accepts Date)
- `frontend/src/lib/auth.ts:20` (`toUTCString()` for cookie)

- [ ] **Step 12: Verify build**

```bash
cd frontend && npm run build
cd ../admin && npm run build
```
Expected: both succeed.

- [ ] **Step 13: Smoke — dev server, hit one detail page, check rendered time**

```bash
cd frontend
npm run dev > /tmp/sprint4-t3-frontend.log 2>&1 &
sleep 8

# 拿一个 post id
curl -s http://localhost:3000/posts/1 | grep -oE '20[0-9]{2}-[0-9]{2}-[0-9]{2}[^<]*' | head -5
```

Expected: 看到 `2026-06-12 14:30` 这样的格式(Asia/Shanghai 时刻),而不是浏览器本地时区。

- [ ] **Step 14: Commit**

```bash
git add frontend/src/lib/date.ts \
        admin/src/lib/date.ts \
        frontend/src/lib/utils.ts \
        frontend/src/components/post/post-card.tsx \
        frontend/src/app/me/messages/page.tsx \
        frontend/src/app/posts/\[id\]/post-detail-content.tsx \
        admin/src/app/companies/page.tsx \
        admin/src/app/users/page.tsx \
        admin/src/app/posts/page.tsx \
        admin/src/app/reports/page.tsx
git commit -m "feat(frontend): SHOULD-36 timezone unified to Asia/Shanghai (intl helpers + 9 file updates)"
```

---

## Final Wrap-up

- [ ] **Step F1: Final integration review**

派遣 final reviewer 一次性看 3 个 commit,确认:
- CI workflow YAML 语法 / jobs 完整 / 触发条件正确
- 批量端点事务原子性 / AuditLog metadata 格式
- 时区 helper 在 9 处都用对了 / SEO/JSON-LD 未被破坏

- [ ] **Step F2: Update docs**

`docs/project-memory.md` 添加 §10.8:
```markdown
### 10.8 P1 Sprint 4 (2026-06-12, V1.0 上线冲刺 4,3 任务完成)

| 任务 | 来源 | Commit | 状态 |
|---|---|---|---|
| T1 | SHOULD-42 GitHub Actions CI(3 job) | (本批) | ✅ backend build + frontend/admin lint+build;后端 lint 注释(无 .eslintrc) |
| T2 | SHOULD-27 批量审核/下架(2 端点) | (本批) | ✅ audit-batch / offline-batch;6 smoke 全 PASS(成功/拒绝/无 reason 400/offline/空 ids 400/单条兼容) |
| T3 | SHOULD-36 时区统一(Intl helper,9 文件) | (本批) | ✅ frontend + admin 各自 date.ts;硬编码 Asia/Shanghai;SEO/JSON-LD 不动 |

**V1.0 P1 关键 20 项完成: 17/20**(Sprint 1+2+3+4 共 18 任务)
```

`docs/development-roadmap.md` Week 10-12 段标记:
- SHOULD-27 ✅
- SHOULD-36 ✅
- SHOULD-42 ✅

汇总:`已 14/20 → 已 17/20`,12 项 → 15 项。

- [ ] **Step F3: Final commit + push**

```bash
git add docs/project-memory.md docs/development-roadmap.md
git commit -m "docs: P1 Sprint 4 complete — update project-memory §10.8 + roadmap Week 10-12"
```

(用户已选不预先 push,等本 sprint 全部完成后再 git push。)

---

## Risk & Rollback Notes

- **T1 CI**:若 workflow 某 job 失败,排查后单独 re-run;**注意**后端 lint 已注释,若未来加 .eslintrc 再 uncomment
- **T2 批量**:`updateMany` 单 SQL,事务短;但 `ids.length = 200` 边界要测;若 audit_logs 写失败,事务回滚,posts 不变
- **T3 时区**:`Intl` API 在 Node 18 + 现代浏览器全支持;若 SSR/CSR ICU 数据差异导致显示不一致(罕见),可改用 `formatToParts` 手动拼;**MVP 阶段 `toLocaleString` 即可**
- **重要**:SEO/JSON-LD 的 ISO Z 字符串**不能**改成本地化(否则 schema.org 不认)
