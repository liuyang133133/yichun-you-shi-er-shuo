# 伊春有事儿说 — 开发任务清单 (TODO)

> 基于《Production Gap Analysis》生成 · 2026-06-24  
> 单位：人天 PD（1 人天 = 8 小时）  
> 状态字段： `[ ]` 待开发 · `[~]` 进行中 · `[x]` 已完成 · `[!]` 阻塞 · `[-]` 废弃

---

## 0. 概览

| 指标 | 数值 |
|---|---|
| **总任务数** | **52** |
| P0 任务 | 28（Sprint 1） |
| P1 任务 | 11（Sprint 2）+ 5（Sprint 3） |
| P2 任务 | 8（Sprint 4） |
| **总开发工作量** | **~190 PD** |
| 总测试工作量 | ~70 PD |
| 总周期 | 20 周（4 个 Sprint） |
| 团队配置建议 | 1 全栈 + 1 后端 + 1 前端 + 0.5 设计 + 0.5 测试 |

### 风险等级定义

| 等级 | 含义 | 上线影响 |
|---|---|---|
| 🔴 极高 | 核心交易链路、数据安全 | 阻塞上线 |
| 🟠 高 | 主流程、用户体验、商业化 | 必做否则功能残缺 |
| 🟡 中 | 增强体验、运营效率 | 可后续迭代 |
| 🟢 低 | 体验锦上添花 | 长期规划 |

### 依赖约定

- 每个任务表里"依赖"列出**必须先完成的**其他任务 ID
- "阻塞"列出**等本任务完成才能开始**的后续任务 ID
- 建议按 Sprint 顺序 + 任务 ID 顺序开发

---

# Sprint 1 — 上线必须（P0）

> 周期：W1–W6（30 工作日） · 工作量：~60 PD  
> 目标：凑齐商业运营最低基础（通知 / 聊天 / RBAC / 标签 / 软删 / 工单 / 评价 / 公告 / 协议）

---

## T-001 软删除 + 审计字段统一 [x] ✅ 2026-06-24

| 字段 | 内容 |
|---|---|
| 任务ID | T-001 |
| 优先级 | P0 |
| 依赖 | — |
| 阻塞 | T-002 / T-007 / T-011 / T-013 / T-019 / T-022 / T-029 |
| 影响数据库 | 22 张业务表全加 `deletedAt` `createdBy` `updatedBy` `deletedBy` + 索引 `@@index([deletedAt])` |
| 影响 API | Prisma middleware 自动过滤 `deletedAt: null`；新增 `POST /admin/posts/:id/restore` 等恢复接口；admin list 默认过滤，加 `?includeDeleted=true` 可见 |
| 影响前端 | 无（透明） |
| 影响后台 | 列表加"包含已删除"复选框；详情加"恢复"按钮 |
| 风险等级 | 🔴 极高（数据完整性） |
| 预计开发 | 3 PD |
| 预计测试 | 2 PD（迁移 + 各表 CRUD 回归） |
| 回归测试 | ✅ 必做（22 表全部） |
| 验收标准 | (1) 22 表迁移成功；(2) `delete` 后 list 默认不可见；(3) admin 加 `?includeDeleted=true` 可见；(4) restore 可恢复；(5) `createdBy/updatedBy/deletedBy` 写入正确 |

---

## T-002 RBAC — 角色 / 权限 / 关联表 [x] ✅ 2026-06-25

| 字段 | 内容 |
|---|---|
| 任务ID | T-002 |
| 优先级 | P0 |
| 依赖 | T-001 |
| 阻塞 | T-003, T-004, T-005, T-006 |
| 影响数据库 | 新增 `Role` `Permission` `UserRole` `RolePermission` 4 张表 |
| 影响 API | 种子 5 个预置角色（super_admin / content_auditor / customer_service / finance / operator） + 权限码 |
| 影响前端 | 无 |
| 影响后台 | 提供数据基础（具体页见 T-004） |
| 风险等级 | 🔴 极高（权限是所有后台功能基础） |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分（用户角色相关） |
| 验收标准 | (1) 4 张表迁移成功；(2) seed 5 角色 + 30+ 权限码；(3) UserRole/RolePermission 增删改查 OK |

---

## T-003 RBAC — 守卫改造（@Roles → @RequirePermission） [x] ✅ 2026-06-25

| 字段 | 内容 |
|---|---|
| 任务ID | T-003 |
| 优先级 | P0 |
| 依赖 | T-002 |
| 阻塞 | — |
| 影响数据库 | 无（仅 seed 扩 8 个权限码 + operator 角色多 5 个码） |
| 影响 API | 10 个 admin controller 全部加 `@RequirePermission`；`@Roles('admin')` 兼容保留 1 个月 |
| 影响前端 | api.ts 加 403 错误处理（`[403] 权限不足`） |
| 影响后台 | 配合 T-004 才能完整生效 |
| 风险等级 | 🔴 极高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ✅ 必做（35+ admin 端点） |
| 验收标准 | (1) 所有 `/admin/*` 端点加 `@RequirePermission`；(2) 无权限返回 403；(3) 旧 `@Roles` 仍可用（兼容期 1 个月） |

| 字段 | 内容 |
|---|---|
| 任务ID | T-003 |
| 优先级 | P0 |
| 依赖 | T-002 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | 替换 `AdminGuard` 中 `@Roles` 校验为 `@RequirePermission('post.audit.pass')` 细粒度；保留 `@Roles` 兼容；失败 403 |
| 影响前端 | 401/403 错误处理统一 |
| 影响后台 | 配合 T-004 才能完整生效 |
| 风险等级 | 🔴 极高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD（每个端点都需权限测试） |
| 回归测试 | ✅ 必做（35+ admin 端点） |
| 验收标准 | (1) 所有 `/admin/*` 端点加 `@RequirePermission`；(2) 无权限返回 403；(3) 旧 `@Roles` 仍可用（兼容期 1 个月） |

---

## T-004 RBAC — 后台角色 / 权限 / 管理员管理 UI [x] ✅ 2026-06-25

| 字段 | 内容 |
|---|---|
| 任务ID | T-004 |
| 优先级 | P0 |
| 依赖 | T-002 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | `GET /admin/users?withRoles=true` 扩展（避免 N+1）|
| 影响前端 | 无 |
| 影响后台 | 3 页面：角色管理 (`/roles`) / 权限管理 (`/permissions`) / 管理员列表 (`/admin-users`) + 系统管理菜单分组 |
| 风险等级 | 🟠 高 |
| 预计开发 | 3 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 3 个菜单可访问；(2) 角色 CRUD + 权限分配；(3) 给用户分配角色生效 |

---

## T-005 后台 — 操作日志查询页 [x] ✅ 2026-06-25

| 字段 | 内容 |
|---|---|
| 任务ID | T-005 |
| 优先级 | P0 |
| 依赖 | T-002 |
| 阻塞 | — |
| 影响数据库 | `AuditLog` 加 `beforeSnapshot` `afterSnapshot` `requestId` `ip` `userAgent` + `@@index([createdAt])` |
| 影响 API | `GET /admin/audit-logs` (7 筛选) + `GET /admin/audit-logs/:id` + `GET /admin/audit-logs/options` + `GET /admin/audit-logs/export` (CSV) |
| 影响前端 | 无 |
| 影响后台 | 新菜单"操作日志"（列表 + 7 筛选 + 详情抽屉 + CSV 导出） |
| 风险等级 | 🟠 高（合规要求） |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 所有写操作自动写入 AuditLog（含 diff）；(2) 7 种筛选组合可用；(3) CSV 导出字段完整 |

---

## T-006 后台 — 登录日志查询页 [x] ✅ 2026-06-25

| 字段 | 内容 |
|---|---|
| 任务ID | T-006 |
| 优先级 | P0 |
| 依赖 | — |
| 阻塞 | — |
| 影响数据库 | 无（LoginLog 已有） |
| 影响 API | `GET /admin/login-logs` (6 筛选) + `:id` + `options` + `abnormal-ips` + `export` |
| 影响前端 | 无 |
| 影响后台 | 新菜单"登录日志"（列表 + 6 筛选 + 失败高亮 + 异常 IP 检测 + CSV 导出） |
| 风险等级 | 🟠 高（安全审计） |
| 预计开发 | 1 PD |
| 预计测试 | 0.5 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 6 种筛选可用；(2) 异常登录（IP 突增）红色标记；(3) CSV 导出 |

---

## T-007 通知系统 — 数据库 + 后端服务 [x] ✅ 2026-06-25

| 字段 | 内容 |
|---|---|
| 任务ID | T-007 |
| 优先级 | P0 |
| 依赖 | T-001 |
| 阻塞 | T-008, T-009 |
| 影响数据库 | 新增 `Notification` `NotificationTemplate` `UserNotificationSetting` `DeviceToken` 4 张表 |
| 影响 API | 9 用户端（notifications + settings + devices） + 内部 `NotificationService.emit(event, payload)` |
| 影响前端 | — |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 3 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 4 张表迁移成功；(2) 8 类事件触发通知；(3) 同步写库（V1 简化，V1.1 接 Bull Queue） |

---

## T-008 通知 — 前端通知中心 + Header 红点 [x] ✅ 2026-06-26

| 字段 | 内容 |
|---|---|
| 任务ID | T-008 |
| 优先级 | P0 |
| 依赖 | T-007 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | 无（复用 T-007）|
| 影响前端 | Header 铃铛 + 红点（30s 轮询）；`/me/notifications` 列表页（tabs + 分页 + 全部已读 + 跳转）；`/me/notifications/settings` 8 事件 + quietHours |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ✅ 必做（header 全局组件） |
| 验收标准 | (1) 通知中心列表 + 已读状态正确；(2) Header 红点实时；(3) 通知设置可保存 |

---

## T-009 通知 — 后台模板管理 + 群发

| 字段 | 内容 |
|---|---|
| 任务ID | T-009 |
| 优先级 | P0 |
| 依赖 | T-007 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | `GET/POST/PATCH/DELETE /admin/notifications/templates` `POST /admin/notifications/broadcast` |
| 影响前端 | — |
| 影响后台 | 新菜单"通知模板"（CRUD + 变量预览 + 启用/停用）；Dashboard 加"群发"快捷入口 |
| 风险等级 | 🟡 中 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 模板 CRUD；(2) 群发按"全员/区域/角色"筛选；(3) 群发记录可查 |

---

## T-010 WebSocket 网关搭建（基础设施）

| 字段 | 内容 |
|---|---|
| 任务ID | T-010 |
| 优先级 | P0 |
| 依赖 | — |
| 阻塞 | T-011, T-012 |
| 影响数据库 | 无 |
| 影响 API | 新增 `WsGateway` 命名空间 `/ws`；JWT 握手鉴权；ping/pong 心跳；断线重连；在线状态广播 |
| 影响前端 | — |
| 影响后台 | — |
| 风险等级 | 🟠 高（基础设施） |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 握手鉴权失败拒绝；(2) 心跳 30s 无响应踢出；(3) 多实例 Redis Adapter 同步 |

---

## T-011 聊天 — 数据库 + API（基于 T-010）

| 字段 | 内容 |
|---|---|
| 任务ID | T-011 |
| 优先级 | P0 |
| 依赖 | T-001, T-010 |
| 阻塞 | T-012 |
| 影响数据库 | 新增 `Conversation`；`Message` 加 `type` `attachmentUrl` `parentId` `conversationId` `deliveredAt` `readBy(json)` |
| 影响 API | WS 事件 `message:send` `message:received` `message:read` `message:typing` `presence:online/offline`；HTTP `GET /messages/conversations` `GET /messages/:conversationId?cursor=` |
| 影响前端 | — |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 3 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 1v1 消息已读回执 ≤ 1s；(2) 敏感词过滤生效；(3) 离线消息推送（站内信） |

---

## T-012 聊天 — 前端聊天窗口

| 字段 | 内容 |
|---|---|
| 任务ID | T-012 |
| 优先级 | P0 |
| 依赖 | T-011 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | — |
| 影响前端 | `/me/messages` 重构为 IM 风格（会话列表 + 聊天窗口 + 正在输入 + 已读回执 + 历史分页加载） |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 3 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ✅ 必做（消息模块完全重构） |
| 验收标准 | (1) 实时收发；(2) 已读状态正确；(3) 历史消息加载无闪烁 |

---

## T-013 标签系统 — 数据库 + 迁移

| 字段 | 内容 |
|---|---|
| 任务ID | T-013 |
| 优先级 | P0 |
| 依赖 | T-001 |
| 阻塞 | T-014, T-015 |
| 影响数据库 | 新增 `Tag` `PostTag`；数据迁移 `Post.tags` JSON → 关系表（保留期 1 个月，1 月后删 JSON 字段） |
| 影响 API | `GET /tags/hot?limit=20` `GET /tags/:slug` `GET /tags/:slug/posts` `POST /tags/suggest`（AI 建议） |
| 影响前端 | — |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分（帖子详情页 SEO 标签链接） |
| 验收标准 | (1) 旧 JSON 标签全部迁移；(2) 同一帖子同一标签不重复（unique 约束）；(3) useCount 自动累加 |

---

## T-014 标签 — 前端选择器 + 标签详情页

| 字段 | 内容 |
|---|---|
| 任务ID | T-014 |
| 优先级 | P0 |
| 依赖 | T-013 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | — |
| 影响前端 | 发布页加标签选择（AI 建议 + 手动输入 + 热门联想）；新增 `/tags/[slug]` 标签详情页（聚合帖 + 描述 + 关注）；首页"热门标签云"组件 |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ✅ 必做（发布页改造） |
| 验收标准 | (1) AI 建议 ≥ 3 个；(2) 标签详情页 SEO OK；(3) 标签云点击跳详情 |

---

## T-015 标签 — 后台管理

| 字段 | 内容 |
|---|---|
| 任务ID | T-015 |
| 优先级 | P0 |
| 依赖 | T-013 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | `GET/POST/PATCH/DELETE /admin/tags` `POST /admin/tags/:id/merge`（合并） |
| 影响前端 | — |
| 影响后台 | 新菜单"标签管理"（列表 + CRUD + 合并 + 别名 + 停用） |
| 风险等级 | 🟡 中 |
| 预计开发 | 1.5 PD |
| 预计测试 | 0.5 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 合并功能把 A 标签的 PostTag 全部转到 B，A 软删除；(2) 停用标签不在前端可见 |

---

## T-016 公告 — 后台 UI

| 字段 | 内容 |
|---|---|
| 任务ID | T-016 |
| 优先级 | P0 |
| 依赖 | — |
| 阻塞 | T-017 |
| 影响数据库 | `Announcement` 加 `targetAudience`（all/area/role）；creator 关系 FK |
| 影响 API | 已有 `GET/POST/PATCH/DELETE /admin/announcements` 补 `targetAudience` 字段；新增 `POST /admin/announcements/:id/push`（触发实时推送） |
| 影响前端 | — |
| 影响后台 | 新菜单"公告管理"（CRUD + 排期 + 推送目标 + 状态预览） |
| 风险等级 | 🟡 中 |
| 预计开发 | 1.5 PD |
| 预计测试 | 0.5 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) CRUD + 排期 OK；(2) targetAudience 筛选正确；(3) 推送触发通知中心 |

---

## T-017 公告 — 前端公告中心 + 首页弹窗

| 字段 | 内容 |
|---|---|
| 任务ID | T-017 |
| 优先级 | P0 |
| 依赖 | T-016 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | — |
| 影响前端 | 首页弹窗（priority 最高的公告，关闭后 24h 不再弹）；`/announcements` 公告中心列表 + 详情 |
| 影响后台 | — |
| 风险等级 | 🟡 中 |
| 预计开发 | 1.5 PD |
| 预计测试 | 0.5 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 弹窗按 priority + 排期生效；(2) 公告列表分页 OK |

---

## T-018 协议页（/terms /privacy /about）

| 字段 | 内容 |
|---|---|
| 任务ID | T-018 |
| 优先级 | P0 |
| 依赖 | — |
| 阻塞 | — |
| 影响数据库 | 新增 `Agreement`（key/version/content/effectiveAt） |
| 影响 API | `GET /agreements/:key`（按 key 返回最新版） |
| 影响前端 | 3 个静态页（Markdown 渲染）；登录页底部链接可点；首次登录弹窗强制同意 |
| 影响后台 | 简版：直接在数据库 seed 即可，V1.1 再做协议管理后台 |
| 风险等级 | 🟠 高（合规） |
| 预计开发 | 1 PD |
| 预计测试 | 0.5 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 3 页面可访问；(2) 登录页链接 200；(3) 首次登录必须勾选才进首页 |

---

## T-019 工单系统 — 数据库 + API

| 字段 | 内容 |
|---|---|
| 任务ID | T-019 |
| 优先级 | P0 |
| 依赖 | T-001 |
| 阻塞 | T-020, T-021 |
| 影响数据库 | 新增 `Ticket` `TicketMessage`；`Ticket.status` enum (pending/replied/closed) |
| 影响 API | `POST /tickets` `GET /tickets/me` `GET /tickets/:id` `POST /tickets/:id/reply` `POST /tickets/:id/close` `GET /admin/tickets` `POST /admin/tickets/:id/assign/reply/close` |
| 影响前端 | — |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 完整工单流程跑通；(2) admin 分配给客服后只能该客服看到 |

---

## T-020 工单 — 前端工单页

| 字段 | 内容 |
|---|---|
| 任务ID | T-020 |
| 优先级 | P0 |
| 依赖 | T-019 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | — |
| 影响前端 | `/me/tickets` 我的工单列表 + 创建 + 详情 + 回复 |
| 影响后台 | — |
| 风险等级 | 🟡 中 |
| 预计开发 | 1.5 PD |
| 预计测试 | 0.5 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 创建/查看/回复工单可用；(2) 状态变化有提示 |

---

## T-021 工单 — 后台工单看板

| 字段 | 内容 |
|---|---|
| 任务ID | T-021 |
| 优先级 | P0 |
| 依赖 | T-019 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | — |
| 影响前端 | — |
| 影响后台 | 新菜单"客服工单"（看板：待处理/处理中/已关闭 + 分配 + SLA 监控 + 标签分类） |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 看板拖拽改状态；(2) 分配后仅该客服可见；(3) SLA 超时标红 |

---

## T-022 评价 — 数据库 + API

| 字段 | 内容 |
|---|---|
| 任务ID | T-022 |
| 优先级 | P0 |
| 依赖 | T-001 |
| 阻塞 | T-023, T-024 |
| 影响数据库 | 新增 `Review` `CreditScore`；`Company` 加 `rating` `reviewCount` |
| 影响 API | `POST /reviews` `GET /posts/:id/reviews` `GET /users/:id/reviews` `GET /users/:id/credit` `GET /companies/:id/reviews` `POST /admin/reviews/:id/hide/restore` |
| 影响前端 | — |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 2.5 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 5 星 + 内容 + 图；(2) 双向评价；(3) 信用分按规则自动算 |

---

## T-023 评价 — 前端评价 UI

| 字段 | 内容 |
|---|---|
| 任务ID | T-023 |
| 优先级 | P0 |
| 依赖 | T-022 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | — |
| 影响前端 | 详情页加评价 Tab（列表 + 发表）；用户主页显示信用分 + 收到评价；公司详情页评分 + 评价列表 |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ✅ 必做（详情页改造） |
| 验收标准 | (1) 评价完整 CRUD；(2) 信用分按评价实时变化；(3) 评价列表分页 |

---

## T-024 评价 — 信用分 + 后台审核

| 字段 | 内容 |
|---|---|
| 任务ID | T-024 |
| 优先级 | P0 |
| 依赖 | T-022 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | `POST /admin/credit/:userId/adjust`（人工干预） |
| 影响前端 | — |
| 影响后台 | 新菜单"评价审核"（差评/投诉列表 + 隐藏/恢复）；"信用分管理"（查询 + 人工调整） |
| 风险等级 | 🟡 中 |
| 预计开发 | 1.5 PD |
| 预计测试 | 0.5 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 差评标红；(2) 隐藏后前端不可见；(3) 人工调整留痕 |

---

## T-025 后台补全 — 分类 / 区域 / 敏感词 / 数据导出

| 字段 | 内容 |
|---|---|
| 任务ID | T-025 |
| 优先级 | P0 |
| 依赖 | T-002（T-025.4 需要 RBAC 校验） |
| 阻塞 | — |
| 影响数据库 | `Category` 加 `slug` `description` `seoTitle` `seoKeywords` `seoDescription` `coverImage` 复合唯一 `@@unique([parentId,code])`；`Area` 加 `status` `slug` `centerLng/Lat` `fullPath` `updatedAt`；新增 `SensitiveWord` 表 |
| 影响 API | `GET/POST/PATCH/DELETE /admin/categories` 已有补字段；`GET/POST/PATCH/DELETE /admin/areas` 新增；`GET/POST/PATCH/DELETE /admin/sensitive-words` 新增；`GET /admin/export/{users|posts|reports|orders}` 导出 CSV |
| 影响前端 | 分类页 tag 化 |
| 影响后台 | 4 个新菜单：分类管理 / 区域管理 / 敏感词管理 / 数据导出（嵌入各列表页） |
| 风险等级 | 🟠 高 |
| 预计开发 | 5 PD |
| 预计测试 | 2 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 4 菜单完整可用；(2) CSV 导出字段完整可导入 Excel |

---

## T-026 修复 8.x 设计不合理（8 个子任务）

| 字段 | 内容 |
|---|---|
| 任务ID | T-026 |
| 优先级 | P0 |
| 依赖 | T-001（部分） |
| 阻塞 | — |
| 影响数据库 | 8.1 全部（enum 化、AuditLog 增强） |
| 影响 API | 8.2 全部（jti crypto、AI 失败重试、敏感词注入 Message、PostService 拆分、4 子表抽基类、定时任务分布式锁、上传尺寸限制、Auth.create 限 admin） |
| 影响前端 | 8.3 全部（middleware、error boundary、refreshToken 自动重试、jobs 页 company 选择、preview URL 配置化、getContact 号码脱敏） |
| 影响后台 | 8.4 全部（公告/分类/区域/敏感词/操作日志/登录日志/批量/导出/图表/审核 prompt 改造） |
| 风险等级 | 🟠 高 |
| 预计开发 | 8 PD（建议拆 8 个 PR，每个 1 PD） |
| 预计测试 | 3 PD |
| 回归测试 | ✅ 必做（全量） |
| 验收标准 | (1) 8.x 列表每项有 issue/PR 闭环；(2) 全量回归无 P0/P1 回归 |

---

## T-027 全量 E2E 测试 + 性能基线

| 字段 | 内容 |
|---|---|
| 任务ID | T-027 |
| 优先级 | P0 |
| 依赖 | T-001 ~ T-026 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | 无 |
| 影响前端 | 无 |
| 影响后台 | 无 |
| 风险等级 | 🟠 高 |
| 预计开发 | 5 PD（写 E2E 用 Playwright，覆盖 12 个关键用户旅程） |
| 预计测试 | 2 PD |
| 回归测试 | ✅ 必做 |
| 验收标准 | (1) 12 个关键旅程 E2E 100% 通过；(2) p95 延迟 < 500ms；(3) Lighthouse > 80 |

---

## T-028 灰度上线 + 监控

| 字段 | 内容 |
|---|---|
| 任务ID | T-028 |
| 优先级 | P0 |
| 依赖 | T-027 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | 灰度开关（按 userId hash） |
| 影响前端 | 灰度提示 banner |
| 影响后台 | 灰度用户管理 |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 5% → 20% → 50% → 100% 分阶段放量；(2) 异常可秒级回滚 |

---

# Sprint 2 — 商业化与运营（P1 + P0-1）

> 周期：W7–W10（20 工作日） · 工作量：~37 PD  
> 目标：启动商业化（支付 / 钱包 / 订单 / 退款）+ 用户中心 + 数据统计 + 系统配置

---

## T-029 支付 — 数据库（Order / Payment / Wallet / Refund / Product）

| 字段 | 内容 |
|---|---|
| 任务ID | T-029 |
| 优先级 | P0 |
| 依赖 | T-001 |
| 阻塞 | T-030, T-031, T-032, T-033, T-034, T-035 |
| 影响数据库 | 新增 `Order` `Payment` `Wallet` `WalletTransaction` `Refund` `Product` 6 张表；`Post.boostExpiresAt` 加 `boostOrderId` 关联 |
| 影响 API | 数据库基础；事务处理 helper |
| 影响前端 | — |
| 影响后台 | — |
| 风险等级 | 🔴 极高（商业化命脉） |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 6 张表迁移；(2) Order 状态机完整；(3) 事务一致性 |

---

## T-030 支付 — 微信支付接入

| 字段 | 内容 |
|---|---|
| 任务ID | T-030 |
| 优先级 | P0 |
| 依赖 | T-029 + 提前 4 周申请微信商户号 |
| 阻塞 | T-032, T-035 |
| 影响数据库 | 无 |
| 影响 API | `POST /payments/orders` `POST /payments/callback/wxpay`（异步通知，验签 + 幂等） `GET /payments/orders/me` `GET /payments/orders/:id` |
| 影响前端 | — |
| 影响后台 | — |
| 风险等级 | 🔴 极高 |
| 预计开发 | 3 PD |
| 预计测试 | 2 PD（沙箱 + 真单各 1 PD） |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) JSAPI / H5 / Native 三端支付成功；(2) 回调验签 + 幂等；(3) 订单状态正确流转 |

---

## T-031 支付 — 支付宝接入

| 字段 | 内容 |
|---|---|
| 任务ID | T-031 |
| 优先级 | P0 |
| 依赖 | T-029 + 提前 4 周申请支付宝商户 |
| 阻塞 | T-032 |
| 影响数据库 | 无 |
| 影响 API | 同 T-030 + `alipay` 渠道 |
| 影响前端 | — |
| 影响后台 | — |
| 风险等级 | 🔴 极高 |
| 预计开发 | 3 PD |
| 预计测试 | 2 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | 同 T-030（支付宝版） |

---

## T-032 支付 — 前端订单/钱包/充值页

| 字段 | 内容 |
|---|---|
| 任务ID | T-032 |
| 优先级 | P0 |
| 依赖 | T-029, T-030, T-031 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | — |
| 影响前端 | `/me/wallet` 钱包页（余额 + 流水 + 充值按钮）；`/me/orders` 订单列表；`/payments/confirm` 支付确认页；`/payments/result` 支付结果页 |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 2.5 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 3 页完整可用；(2) 充值成功入账；(3) 订单状态实时刷新 |

---

## T-033 支付 — 退款流程

| 字段 | 内容 |
|---|---|
| 任务ID | T-033 |
| 优先级 | P0 |
| 依赖 | T-030, T-031 |
| 阻塞 | — |
| 影响数据库 | `Refund` 已有 |
| 影响 API | `POST /payments/orders/:id/refund`（用户申请） `POST /admin/payments/orders/:id/refund`（admin 审批） |
| 影响前端 | 订单详情加"申请退款"按钮 + 退款详情 |
| 影响后台 | 退款审批页（待处理/已通过/已拒绝） |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 用户申请 → admin 审批 → 实际退款 → 余额入账；(2) 状态可追踪 |

---

## T-034 后台 — 订单 / 退款管理

| 字段 | 内容 |
|---|---|
| 任务ID | T-034 |
| 优先级 | P0 |
| 依赖 | T-029 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | `GET/POST /admin/payments/orders` `GET /admin/payments/refunds` |
| 影响前端 | — |
| 影响后台 | 新菜单"订单管理"（订单列表 + 详情 + 强制退款 + 改状态）；"退款管理"（审批 + 流水） |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 订单可筛选（状态/支付方式/时间/用户）；(2) 退款流程完整 |

---

## T-035 Boost 接支付（信息置顶商业化）

| 字段 | 内容 |
|---|---|
| 任务ID | T-035 |
| 优先级 | P0 |
| 依赖 | T-030, T-031 |
| 阻塞 | — |
| 影响数据库 | `Post.boostExpiresAt` 已有 + `boostOrderId` 关联 |
| 影响 API | 替换 `PostBoostService.boost()` 之前的 404 stub；接 Order 创建 + 支付回调后自动置顶 |
| 影响前端 | BoostCta 组件接支付（已有 UI，替换为真实支付流） |
| 影响后台 | Boost 订单管理（嵌入 T-034 订单页 filter） |
| 风险等级 | 🟠 高 |
| 预计开发 | 1.5 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 支付成功 + boost 生效 + 列表置顶；(2) 过期自动取消 boost |

---

## T-036 Banner 投放商业化

| 字段 | 内容 |
|---|---|
| 任务ID | T-036 |
| 优先级 | P1 |
| 依赖 | T-029, T-030 |
| 阻塞 | — |
| 影响数据库 | `Banner` 加 `startDate` `endDate` `planType` (cpc/cpm/cpd) `dailyBudget` `totalBudget` `spent` `clicks` `impressions` `advertiserId` |
| 影响 API | `POST /admin/banners/:id/stats` 跟踪 CTR |
| 影响前端 | Banner 前端加点击埋点 |
| 影响后台 | Banner 列表加投放计划维度（已嵌入 `/banners` 改造） |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) Banner 可投放到期；(2) CTR 数据正确 |

---

## T-037 用户中心 — 5 个页

| 字段 | 内容 |
|---|---|
| 任务ID | T-037 |
| 优先级 | P1 |
| 依赖 | — |
| 阻塞 | — |
| 影响数据库 | 新增 `UserBrowseHistory` `Follow`（复用 P1-8 部分） `UserCoupon`（V1.1） |
| 影响 API | `GET /users/me/browse-history` `POST /users/me/followings` `GET /users/me/coupons` |
| 影响前端 | 5 个新页：`/me/orders`（订单复用 T-032） `/me/browse-history` `/me/followings` `/me/coupons`（占位） `/me/wallet`（复用 T-032） |
| 影响后台 | — |
| 风险等级 | 🟡 中 |
| 预计开发 | 3 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 5 个页可用；(2) 数据正确 |

---

## T-038 后台 — 数据统计看板

| 字段 | 内容 |
|---|---|
| 任务ID | T-038 |
| 优先级 | P1 |
| 依赖 | — |
| 阻塞 | — |
| 影响数据库 | 已有表聚合查询 |
| 影响 API | `GET /admin/stats/overview?from=&to=` `GET /admin/stats/users/retention?cohort=` `GET /admin/stats/funnel?type=` `GET /admin/stats/revenue?from=&to=` |
| 影响前端 | — |
| 影响后台 | 改造 `/dashboard`（折线图 + 漏斗 + cohort 表格 + 留存图）；新菜单"数据分析"（DAU/MAU/留存/漏斗/收入/区域） |
| 风险等级 | 🟠 高 |
| 预计开发 | 4 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 折线/柱状/漏斗/cohort 4 类图表；(2) 数据准确（与 DB 抽样一致） |

---

## T-039 后台 — 系统配置中心

| 字段 | 内容 |
|---|---|
| 任务ID | T-039 |
| 优先级 | P1 |
| 依赖 | T-002 |
| 阻塞 | — |
| 影响数据库 | 新增 `SystemConfig` KV + `NotificationTemplate` 已在 T-009 |
| 影响 API | `GET/PUT /admin/config/site` `GET/PUT /admin/config/sms-templates` `GET/PUT /admin/config/email-templates` `GET/PUT /admin/config/feature-flags` `GET/PUT /admin/config/api-keys`（加密存储） |
| 影响前端 | — |
| 影响后台 | 新菜单"系统配置"（Tab：站点 / 短信 / 邮件 / 推送 / API Key / Feature Flag） |
| 风险等级 | 🟡 中 |
| 预计开发 | 3 PD |
| 预计测试 | 1 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 配置可热更新（短信模板免重启）；(2) API Key 加密存储 |

---

# Sprint 3 — 生态扩展（P1 + P2-1）

> 周期：W11–W14（20 工作日） · 工作量：~38 PD  
> 目标：地图 / 商家 / 推荐 / 审核 / 关注 / 搜索升级

---

## T-040 高德地图集成

| 字段 | 内容 |
|---|---|
| 任务ID | T-040 |
| 优先级 | P1 |
| 依赖 | 提前 1 周申请高德 Key |
| 阻塞 | — |
| 影响数据库 | `PostHouse` 已有 `lng/lat` 启用 |
| 影响 API | `GET /posts?near=lng,lat&radius=km` |
| 影响前端 | 房屋列表加地图视图（marker 聚合）；详情页加地图定位 + 附近 POI；筛选加"附近 N km" |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 4 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 地图选房可用；(2) 附近筛选正确；(3) 性能：1000 marker < 1s |

---

## T-041 商家入驻完整化

| 字段 | 内容 |
|---|---|
| 任务ID | T-041 |
| 优先级 | P1 |
| 依赖 | T-038（统计） |
| 阻塞 | — |
| 影响数据库 | `Company` 加 `businessLicenseNo` `businessLicenseImg` `verifiedAt` `verifiedBy` `contactPhone` `contactEmail` `website` `status` `slug` `rating` `reviewCount`；新增 `CompanyLevel` enum (normal/silver/gold/flagship) |
| 影响 API | `POST /companies/onboard`（注册 + 营业执照上传） `GET /admin/companies/pending` 入驻审批列表 |
| 影响前端 | `/me/company` 商家后台 v1（店铺信息 / 资质 / 招聘 / 评价 / 数据） |
| 影响后台 | 新菜单"商家入驻"（待审 / 已通过 / 已驳回 + 等级调整） |
| 风险等级 | 🟠 高 |
| 预计开发 | 5 PD |
| 预计测试 | 2 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 完整入驻流程跑通；(2) 商家后台 5 个 Tab 可用 |

---

## T-042 推荐系统 MVP

| 字段 | 内容 |
|---|---|
| 任务ID | T-042 |
| 优先级 | P1 |
| 依赖 | T-013（标签） T-038（统计） |
| 阻塞 | — |
| 影响数据库 | 新增 `UserRecommend` Redis ZSET 缓存 |
| 影响 API | `GET /posts/recommend?userId=`（基于标签 + 区域 + 浏览历史加权） |
| 影响前端 | 首页 `?type=` 时插入"猜你想看"区块（个性化） |
| 影响后台 | 开关 + 调权 UI |
| 风险等级 | 🟡 中 |
| 预计开发 | 4 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 推荐 CTR > 普通列表 20%；(2) 性能 < 200ms |

---

## T-043 内容审核增强（接入阿里云内容安全）

| 字段 | 内容 |
|---|---|
| 任务ID | T-043 |
| 优先级 | P1 |
| 依赖 | 提前 2 周申请阿里云 Key |
| 阻塞 | — |
| 影响数据库 | `Post.auditStatus` 加 `machine_rejected` 状态 |
| 影响 API | `post.service.create` 前置调用阿里云 green（文本 + 图片） |
| 影响前端 | 发布失败时提示"内容含敏感信息" |
| 影响后台 | 审核页加"机器审核置信度"列；机器拒绝可人工复审 |
| 风险等级 | 🟠 高 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ✅ 必做（发布流） |
| 验收标准 | (1) 涉政/涉黄/广告自动拦；(2) 误判率 < 5% |

---

## T-044 关注系统

| 字段 | 内容 |
|---|---|
| 任务ID | T-044 |
| 优先级 | P1 |
| 依赖 | — |
| 阻塞 | — |
| 影响数据库 | 新增 `Follow`（followerId + followeeId + targetType + createdAt） |
| 影响 API | `POST /follows` `DELETE /follows/:id` `GET /users/:id/followers` `GET /users/:id/followings` `GET /me/feed`（关注流） |
| 影响前端 | 用户主页加关注按钮；`/me/feed` 关注动态流（聚合用户/标签/分类关注） |
| 影响后台 | 关注关系图谱查询（V1.2） |
| 风险等级 | 🟡 中 |
| 预计开发 | 3 PD |
| 预计测试 | 1 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 关注/取关生效；(2) 关注流聚合正确 |

---

## T-045 搜索升级 Meilisearch

| 字段 | 内容 |
|---|---|
| 任务ID | T-045 |
| 优先级 | P1（提前 P2-1） |
| 依赖 | — |
| 阻塞 | — |
| 影响数据库 | 无（外部搜索引擎） |
| 影响 API | `GET /search` 切换为 Meilisearch；保留 MySQL FT 兜底 |
| 影响前端 | 搜索结果高亮 + 纠错建议 + 分面筛选（按区域/分类/价格区间） |
| 影响后台 | 重建索引按钮 + 同步状态监控 |
| 风险等级 | 🟠 高 |
| 预计开发 | 4 PD |
| 预计测试 | 1.5 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 100w 数据搜索 < 100ms；(2) 高亮 + 纠错可用 |

---

# Sprint 4 — 长线能力（P2）

> 周期：W15–W20（30 工作日） · 工作量：~55 PD  
> 目标：H5 / 短视频 / 担保交易 / 可观测性 / 测试覆盖

---

## T-046 H5 微信内置浏览器适配

| 字段 | 内容 |
|---|---|
| 任务ID | T-046 |
| 优先级 | P1 |
| 依赖 | 提前 2 周申请微信公众号 |
| 阻塞 | — |
| 影响数据库 | `User` 加 `wechatOpenId` `wechatUnionId` |
| 影响 API | `POST /auth/wechat-login` 微信 OAuth；JS-SDK 签名接口 |
| 影响前端 | 微信内置浏览器适配（OAuth 登录 / 分享卡片 / 支付）；PWA（添加到桌面 / 离线壳） |
| 影响后台 | — |
| 风险等级 | 🟠 高 |
| 预计开发 | 4 PD |
| 预计测试 | 2 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 微信登录可用；(2) 分享卡片正常显示 |

---

## T-047 收藏夹分组

| 字段 | 内容 |
|---|---|
| 任务ID | T-047 |
| 优先级 | P1 |
| 依赖 | — |
| 阻塞 | — |
| 影响数据库 | 新增 `FavoriteFolder`；`Favorite` 加 `folderId` |
| 影响 API | `GET/POST/PATCH/DELETE /favorite-folders` `POST /favorites?folderId=` |
| 影响前端 | `/me/favorites` 改造：左侧文件夹树 + 右侧收藏列表 |
| 影响后台 | — |
| 风险等级 | 🟢 低 |
| 预计开发 | 1.5 PD |
| 预计测试 | 0.5 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 文件夹 CRUD；(2) 收藏可分组 |

---

## T-048 草稿 / 定时发布

| 字段 | 内容 |
|---|---|
| 任务ID | T-048 |
| 优先级 | P1 |
| 依赖 | — |
| 阻塞 | — |
| 影响数据库 | `Post` 加 `scheduledAt` 字段；已有 `status='draft'` |
| 影响 API | `POST /posts/scheduled` 定时发布；cron 每分钟扫描发布 |
| 影响前端 | 发布页加"保存草稿" / "定时发布"按钮；`/me/posts/drafts` 草稿箱 |
| 影响后台 | 定时任务监控 |
| 风险等级 | 🟡 中 |
| 预计开发 | 2 PD |
| 预计测试 | 1 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) 草稿可保存可恢复；(2) 定时发布误差 < 1min |

---

## T-049 短视频 / 直播

| 字段 | 内容 |
|---|---|
| 任务ID | T-049 |
| 优先级 | P2 |
| 依赖 | T-029（付费） + 提前 4 周选 CDN 厂商 |
| 阻塞 | — |
| 影响数据库 | 新增 `Video` `LiveRoom` `LiveViewer` |
| 影响 API | 视频上传（OSS + 转码） + 直播推流/拉流地址生成 |
| 影响前端 | 视频播放器 + 上传组件；直播间 UI + 弹幕 |
| 影响后台 | 视频/直播审核 + 举报 + 数据 |
| 风险等级 | 🟠 高（复杂度高） |
| 预计开发 | 10 PD |
| 预计测试 | 4 PD |
| 回归测试 | ⚠️ 部分 |
| 验收标准 | (1) 视频上传 + 转码 + 播放；(2) 直播间 1000 并发不卡 |

---

## T-050 担保交易（仅二手交易）

| 字段 | 内容 |
|---|---|
| 任务ID | T-050 |
| 优先级 | P2 |
| 依赖 | T-029 T-030 T-031（支付基础） |
| 阻塞 | — |
| 影响数据库 | 新增 `EscrowOrder`（担保单）`Shipment`（物流）`ConfirmReceipt`（确认收货） |
| 影响 API | 二手交易全流程：拍下 → 付款（冻结） → 发货 → 确认收货（放款） → 7 天自动确认 |
| 影响前端 | 二手详情页加"担保购买"按钮 + 担保订单页 + 物流跟踪 |
| 影响后台 | 担保订单管理 + 纠纷介入 |
| 风险等级 | 🔴 极高（资金安全） |
| 预计开发 | 8 PD |
| 预计测试 | 3 PD |
| 回归测试 | ✅ 必做（资金链路） |
| 验收标准 | (1) 全流程跑通；(2) 7 天自动确认；(3) 纠纷可客服介入 |

---

## T-051 Sentry + Prometheus + 告警

| 字段 | 内容 |
|---|---|
| 任务ID | T-051 |
| 优先级 | P2 |
| 依赖 | 提前 1 周申请 Sentry 账号 |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | `/metrics` Prometheus 端点 |
| 影响前端 | 前端 Sentry SDK 接入 |
| 影响后台 | Grafana 看板 + AlertManager 告警规则（错误率 / 延迟 / 队列积压） |
| 风险等级 | 🟠 高 |
| 预计开发 | 2.5 PD |
| 预计测试 | 1 PD |
| 回归测试 | ❌ 不需要 |
| 验收标准 | (1) Sentry 捕获前后端错误；(2) Prometheus 指标完整；(3) 关键告警规则 10+ 条 |

---

## T-052 测试覆盖提升至 70%

| 字段 | 内容 |
|---|---|
| 任务ID | T-052 |
| 优先级 | P2 |
| 依赖 | T-027 基础 E2E |
| 阻塞 | — |
| 影响数据库 | 无 |
| 影响 API | 无 |
| 影响前端 | 无 |
| 影响后台 | 无 |
| 风险等级 | 🟠 高 |
| 预计开发 | 5 PD（补 auth/payment/comment/report/search/upload 单测） |
| 预计测试 | 2 PD |
| 回归测试 | ✅ 必做 |
| 验收标准 | (1) 后端单测覆盖 ≥ 70%；(2) E2E 12 个关键旅程 100% 通过 |

---

# 附录

## A. 任务依赖总图（Sprint 1）

```
T-001 (软删除)
├── T-002 (RBAC 表)
│   ├── T-003 (守卫改造)
│   ├── T-004 (角色后台)
│   └── T-025.4 (分类/区域/敏感词 RBAC 校验)
├── T-007 (通知表)
│   ├── T-008 (通知前端)
│   └── T-009 (通知模板后台)
├── T-011 (聊天表)
│   └── T-012 (聊天前端)
├── T-013 (标签表)
│   ├── T-014 (标签前端)
│   └── T-015 (标签后台)
├── T-019 (工单表)
│   ├── T-020 (工单前端)
│   └── T-021 (工单后台)
├── T-022 (评价表)
│   ├── T-023 (评价前端)
│   └── T-024 (评价后台)
└── T-029 (支付表 - Sprint 2)

T-010 (WebSocket 网关) → T-011 (聊天)
T-016 (公告后台) → T-017 (公告前端)
T-018 (协议页) — 独立
T-026 (修 8.x) — 并行
T-027 (E2E) → T-028 (灰度)
```

## B. 任务依赖总图（Sprint 2-4）

```
T-029 (支付表) → T-030 (微信) → T-035 (Boost)
                  → T-031 (支付宝)
                  → T-032 (前端) → T-033 (退款)
                  → T-034 (后台)
                  → T-036 (Banner)
                  → T-050 (担保 - Sprint 4)
T-038 (统计) → T-041 (商家 - Sprint 3) → T-042 (推荐)
T-013 (标签) → T-042 (推荐 - Sprint 3)
T-039 (系统配置) — 独立
T-040 (地图) - 独立
T-043 (内容审核) - 独立
T-044 (关注) - 独立
T-045 (Meilisearch) - 独立
T-046 (H5) - 独立
T-049 (视频/直播) - 独立
T-051 (可观测) - 独立
T-052 (测试覆盖) - 依赖 T-027
```

## C. 风险等级分布

| 等级 | 任务数 | 任务 |
|---|---|---|
| 🔴 极高 | 5 | T-001, T-002, T-003, T-029, T-030, T-031, T-050 |
| 🟠 高 | 18 | T-005, T-006, T-007, T-010, T-011, T-012, T-013, T-014, T-016, T-018, T-019, T-021, T-022, T-023, T-025, T-026, T-027, T-028, T-032, T-033, T-034, T-035, T-036, T-038, T-040, T-041, T-043, T-045, T-046, T-049, T-051, T-052 |
| 🟡 中 | 8 | T-008, T-009, T-015, T-017, T-020, T-024, T-037, T-039, T-042, T-044, T-048 |
| 🟢 低 | 1 | T-047 |

## D. 第三方依赖提前申请清单

| 依赖 | 提前申请 | 阻塞任务 |
|---|---|---|
| 微信商户号 | 4 周 | T-030 |
| 支付宝商户 | 4 周 | T-031 |
| 微信公众平台 | 2 周 | T-046 |
| 高德地图 Key | 1 周 | T-040 |
| 阿里云内容安全 | 2 周 | T-043 |
| 阿里云 OSS / CDN | 4 周 | T-049 |
| Sentry 账号 | 1 周 | T-051 |
| Meilisearch / 云服务 | 1 周 | T-045 |
| 域名 / ICP 备案 | 4 周 | T-028 |

## E. 任务状态总览（更新于 2026-06-24）

| 状态 | 任务数 |
|---|---|
| ✅ 已完成 | 0 |
| 🚧 进行中 | 0 |
| 📋 待开发 | 52 |
| 🚫 阻塞 | 0 |
| ❌ 废弃 | 0 |

## F. 验收标准模板

每个任务完成时，确认：
- [ ] 开发完成（功能 / API / 页面全部按任务表实现）
- [ ] 单元测试通过（后端 ≥ 60% 覆盖）
- [ ] E2E 测试通过（如有相关用户旅程）
- [ ] 代码 review 通过
- [ ] 风险评估中标注的回归测试已执行
- [ ] 文档更新（CHANGELOG.md / API 文档 / 部署文档）
- [ ] 关联任务可开始（如解除阻塞）

## G. 周报模板（建议）

每周更新一次任务状态：
- 本周完成：T-NNN, T-NNN
- 下周计划：T-NNN, T-NNN
- 阻塞 / 风险：...
- 验收 / 上线决策：...

---

> 维护规则：
> 1. 每个 Sprint 结束后做一次"实际 vs 估算"复盘，更新 PD 估算
> 2. 阻塞超过 3 天的任务升级到 Sprint owner
> 3. 新增需求必须先评估对现有任务依赖的影响
> 4. 每月一次 Roadmap 调整会议，更新 TODO.md
